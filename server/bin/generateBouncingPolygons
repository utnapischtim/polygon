#!/bin/bash

phases=9

for nodes in {100..103..1}
do
  for reflex in {1..3..1}
  do
    echo "nodes: $nodes, reflex: $reflex"
    ./bin/polygon --generator=8 --output-format=gnuplot --reflex-points=$reflex --file-base=polygon --nodes=$nodes --phases=$phases --output-dir=out
  done
done
